# 财务 ERP 上线准备

当前 CI/CD 使用 **GitHub Actions**，只构建并上传产物，**不会自动部署**。上线是手动把 jar 和前端 `dist` 放到服务器，用 Nginx 反代 `/api`。

| 仓库 | 产物 |
| --- | --- |
| [financial-erp](https://github.com/Superljf/financial-erp/actions) | `financial-erp-jar` |
| [financial-erp-Web](https://github.com/Superljf/financial-erp-Web/actions) | `platform-dist`（管理端）、`company-dist`（公司端） |

Artifacts 保留 **14 天**，服务器上请自行留一份备份。

## 1. 服务器

一台 Linux（建议 2 核 4G），安装：

- JDK 17
- MySQL 8
- Redis
- Nginx
- 两个域名（或两个端口）：管理端、公司端

建议目录：

```text
/opt/financial-erp/app.jar
/opt/financial-erp/uploads/
/opt/financial-erp/financial-erp.env
/var/www/financial-erp/platform/
/var/www/financial-erp/company/
```

```bash
sudo mkdir -p /opt/financial-erp/uploads /var/www/financial-erp/platform /var/www/financial-erp/company
sudo useradd -r -s /usr/sbin/nologin erp || true
sudo chown -R erp:erp /opt/financial-erp
```

不要把 `8088` 暴露到公网，只给本机 Nginx 访问。

## 2. 下载产物

打开仓库 **Actions** → 选一次成功的 `ci-cd` → **Artifacts**：

1. 后端下载 `financial-erp-jar`，放到 `/opt/financial-erp/app.jar`（或保留原名）。
2. 前端下载 `platform-dist`、`company-dist`，分别解压到 `/var/www/financial-erp/platform/` 与 `/var/www/financial-erp/company/`（目录里应能直接看到 `index.html`）。

## 3. MySQL 与 Redis

```sql
CREATE DATABASE IF NOT EXISTS financial_erp
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

CREATE USER IF NOT EXISTS 'erp'@'localhost' IDENTIFIED BY '换成强密码';
GRANT ALL PRIVILEGES ON financial_erp.* TO 'erp'@'localhost';
FLUSH PRIVILEGES;
```

确认 Redis 在跑（验证码依赖它，连不上获取验证码会 500）：

```bash
sudo systemctl enable --now redis
redis-cli ping   # 应返回 PONG
```

首次启动后端时 Flyway 会自动建表，**不要手工执行** `V1__init_schema.sql`。

## 4. 环境变量（必须改默认值）

写入 `/opt/financial-erp/financial-erp.env`（权限 `600`，不要提交到 Git）：

```bash
SPRING_PROFILES_ACTIVE=prod
DB_URL=jdbc:mysql://127.0.0.1:3306/financial_erp?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
DB_USERNAME=erp
DB_PASSWORD=换成强密码
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
JWT_SECRET=至少32字节的随机串
PASSWORD_ENCRYPTION_KEY=另一段随机串且不要与JWT相同
UPLOAD_DIR=/opt/financial-erp/uploads
```

生成随机串示例：

```bash
openssl rand -base64 48
```

| 变量 | 说明 |
| --- | --- |
| `JWT_SECRET` | 签发登录 Token，泄露后任何人可伪造身份 |
| `PASSWORD_ENCRYPTION_KEY` | 公司专用密码明文回填的 AES 密钥。**写入过公司密码后不要改**，改了管理端无法解密 |
| `UPLOAD_DIR` | 导入 Excel 快照目录，需可写 |

不要使用仓库里的开发默认密钥，不要提交 `application-local.yml`。

## 5. 启动后端（systemd）

`/etc/systemd/system/financial-erp.service`：

```ini
[Unit]
Description=Financial ERP backend
After=network.target mysql.service redis.service

[Service]
User=erp
Group=erp
EnvironmentFile=/opt/financial-erp/financial-erp.env
WorkingDirectory=/opt/financial-erp
ExecStart=/usr/bin/java -jar /opt/financial-erp/app.jar --spring.profiles.active=prod
SuccessExitStatus=143
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now financial-erp
sudo journalctl -u financial-erp -f
```

日志出现 `Tomcat started on port 8088` 即成功。

种子管理员是 `admin / admin123`，**登录后立刻改密码**。

## 6. Nginx

前端请求使用相对路径 `/api`，必须和页面同源反代。不要把接口地址写进前端构建产物，也不要用 GitHub Pages。

导入 Excel 最大 20MB，需要 `client_max_body_size`。

`/etc/nginx/conf.d/financial-erp.conf`：

```nginx
# 管理端
server {
    listen 80;
    server_name admin.example.com;
    root /var/www/financial-erp/platform;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8088;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        client_max_body_size 20m;
        proxy_read_timeout 120s;
    }
}

# 公司端
server {
    listen 80;
    server_name company.example.com;
    root /var/www/financial-erp/company;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8088;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        client_max_body_size 20m;
        proxy_read_timeout 120s;
    }
}
```

把 `admin.example.com` / `company.example.com` 换成真实域名，解析到服务器 IP。

```bash
sudo nginx -t && sudo systemctl reload nginx
```

有证书后用 443，并把 80 跳转到 HTTPS。

只有一台机器、暂时没有域名时，可用两个端口代替两个 `server_name`（例如 `listen 8080` 管理端、`listen 8081` 公司端）。

## 7. 验收清单

- [ ] 管理端、公司端页面能打开，刷新子路由不 404
- [ ] 图形验证码能出来（Redis 正常）
- [ ] 管理员登录成功，已修改 `admin123`
- [ ] 新建一家公司，公司端能用「公司名称 + 专用账号 + 密码」登录
- [ ] 导入一份小 Excel，`UPLOAD_DIR` 有文件，库里有当月数据
- [ ] 公网打不开 `http://服务器IP:8088` 和 `/swagger-ui.html`
- [ ] `8088` 仅监听本机，或防火墙未放行

## 8. 日常更新

1. 等 GitHub Actions 成功。
2. 下载新 jar / 新 dist。
3. 替换文件：

```bash
sudo systemctl stop financial-erp
sudo cp financial-erp-1.0.0-SNAPSHOT.jar /opt/financial-erp/app.jar
sudo systemctl start financial-erp

# 前端：清空后解压新 dist，避免残留旧 chunk
sudo rm -rf /var/www/financial-erp/platform/*
sudo unzip platform-dist.zip -d /var/www/financial-erp/platform/
sudo rm -rf /var/www/financial-erp/company/*
sudo unzip company-dist.zip -d /var/www/financial-erp/company/
```

Flyway 会在启动时执行新的 `V*__*.sql`。更新前请备份 MySQL。

## 9. 不要做的事

- 不要把生产密码、`financial-erp.env`、`application-local.yml` 提交进仓库
- 不要用 GitHub Pages 托管前端（无法反代 `/api`）
- 不要改已经用于生产的 `PASSWORD_ENCRYPTION_KEY`
- 不要手工建表或重复执行迁移脚本
- 不要依赖 Actions Artifacts 做长期备份

服务器和域名就绪后，可以把 `SSH_HOST` / `SSH_USER` / `SSH_KEY` 配进 GitHub Secrets，再接自动发布。
