# 本地演示与数据说明

本文整理自项目沟通结论。云服务器正式上线见 [上线准备.md](./上线准备.md)。当前约定：**CI 只用 GitHub Actions**。

数据在本机 **MySQL**（后端项目库 `financial_erp`），不在本前端仓库。命令行查库、表结构说明见后端仓库的 `本地演示与数据.md`。

## 1. 给别人看，成本最小

| 场景 | 做法 |
| --- | --- |
| 自己用 | `platform` → localhost:5173，`company` → localhost:5174 |
| 给别人看、尽量不花钱 | 家里电脑 + Cloudflare Tunnel（先发管理端） |
| 公司正式用、要域名 | 国内云 + 备案，见 [上线准备.md](./上线准备.md) |

shadcn / Vercel **不能**单独撑起本系统：Vercel 挂不了长期运行的 Spring Boot，也替代不了 `/api` 反代。

公司端目前仍有 mock 登录，给外人看真业务请先用 **管理端 platform**。

## 2. Cloudflare Tunnel（管理端）

1. 本机 MySQL、Redis、后端 8088、`cd platform && npm run dev` 先自己登录成功。
2. 若隧道打开后 403，在 `platform/vite.config.ts` 的 `server` 增加 `allowedHosts: true`（或 `['.trycloudflare.com']`），保留 `/api` 代理到 `8088`。
3. 电脑关闭睡眠。

```powershell
winget install --id Cloudflare.cloudflared
cloudflared tunnel --url http://127.0.0.1:5173
```

把出现的 `https://xxxx.trycloudflare.com` 发给别人。窗口、后端、数据库都不能关。快速隧道每次启动换链接。

不要映射 3306 / 8088；演示完关掉 cloudflared。不要放真实财务数据。

## 3. 数据和 DBeaver

没有 DBeaver **不影响**演示。公司主数据在 MySQL 表 `company`。原始汇总导入会自动建公司。查库请到后端项目用 `mysql -u root -p` 后 `USE financial_erp;`。
